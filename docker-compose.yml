services:
  dnserve:
    build: .
    container_name: dnserve
    ports:
      - "53:53/udp"
      - "53:53/tcp"
      - "9100:9100/tcp"
    cap_add:
      - NET_BIND_SERVICE
    restart: unless-stopped
